"""JARVIS — a voice-driven, autonomous Claude Code companion.

This package wraps the Claude Agent SDK (the engine behind Claude Code) with a
persona, an event bus, a state machine, and — in later phases — voice, a dashboard,
and Telegram presence. Phase 0 ships the brain in a text REPL.
"""

__version__ = "0.1.0"
