# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenv
.venv/
venv/

# Jarvis runtime state & secrets
.jarvis/state/
.jarvis/.env
.env

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Models / caches downloaded at runtime (Phase 1)
*.onnx
*.pt

# Source media (large, third-party; consumed into ~/.jarvis/sounds — not distributed)
*.mp4
*.mov

# NOTE: the voice-clone reference clip (jarvis-voice.wav) IS committed to this PRIVATE repo so
# every collaborator gets the same voice. Keep this repository PRIVATE — it carries a reference
# voice that must not be redistributed. Other .wav files stay ignored.
*.wav
!jarvis-voice.wav

# Stray test artifact
hello.txt

# Stray generated document inside the package dir
*.docx
